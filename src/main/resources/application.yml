spring:
  profiles:
    active: mysql
  batch:
    job:
      names: simpleJob
      #names: ${job.name:NONE}
---
spring:
  datasource:
    hikari:
      jdbc-url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;MODE=MYSQL;
      username: sa
      password:
      driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: none
  config:
    activate:
      on-profile:
---
spring:
  datasource:
    hikari:
      jdbc-url: jdbc:mysql://server.dbsg.co.kr:3306/spring_batch
      username: everywhere
      password: everypw
      driver-class-name: com.mysql.cj.jdbc.Driver
  config:
    activate:
      on-profile: